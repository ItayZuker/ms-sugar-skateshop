import React from "react"
import "./d-s-prod-prev-price.scss"

const DSProdPrevPrice = ({ tab }) => {

    /* JSX */
    return (
        <div className={"d-s-prod-prev-price-container " + (tab === "filters" ? "open" : "")}>

        </div>
    )
}

export default DSProdPrevPrice